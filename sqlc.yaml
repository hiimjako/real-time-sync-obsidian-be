version: "2"
sql:
  - engine: "sqlite"
    schema: "./migrations"
    queries: "./query"
    gen:
      go:
        emit_json_tags: true
        package: "repository"
        out: "internal/repository"
